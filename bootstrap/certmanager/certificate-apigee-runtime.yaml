apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apigee-runtime-cert
  namespace: apigee
spec:
  secretName: apigee-runtime-tls
  issuerRef:
    name: apigee-selfsigned
    kind: ClusterIssuer
  dnsNames:
    - api.example.com